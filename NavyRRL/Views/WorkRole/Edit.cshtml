@using Models.Schema;
@using Newtonsoft.Json;
@model WorkRole
@{
	var title = Model.Id > 0 ? "Editing Functional Area" : "New Functional Area";
	ViewBag.Title = title;
}

@Html.Partial( "~/Views/Shared/_FormHelperV1.cshtml" )

<script type="text/javascript">
	var Data = {
		//The original resource
		Source: @Html.Raw( JsonConvert.SerializeObject( Model, Formatting.None ) ),
		//Mapping used internally for the FormItem stuff
		Form: {},
		//Functions to configure FormItem stuff
		Config: {
			//Configure Save Button
			SaveButton: function (Field) {
				Field.SaveURL = "@Url.Content("~/workrole/save/")";
				Field.RedirectAfterSuccessURL = "@Url.Content("~/workrole/detail/")";
				Field.getDataToSave = function () {
					return {
						...Data.Source,
						...Data.Form.read()
					};
				}
			}
		}
	};

	$(document).ready(function () {
		FormItem.setupFormItems(Data.Form, "WorkRole", Data.Config);
		Data.Form.write(Data.Source);
	});
	//
</script>

<h2>@title</h2>

<form-item data-path="WorkRole.Id" data-ui="display" data-label="ID"></form-item>
<form-item data-path="WorkRole.RowId" data-ui="display" data-label="RowID"></form-item>
<form-item data-path="WorkRole.Name" data-ui="text" data-label="Name"></form-item>
<form-item data-path="WorkRole.CodedNotation" data-ui="text" data-label="Code"></form-item>
@*<form-item data-path="WorkRole.Description" data-ui="textarea" data-label="Description"></form-item>*@
<form-item data-path="WorkRole.SaveButton" data-ui="savebutton" data-label="Save Changes" data-config="SaveButton"></form-item>